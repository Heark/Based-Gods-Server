def { init }
{ begin } 
start if { bar.f =  X_to_i.new(if file ~!.it) } return y
if { begin }.start?
  end.file_processing
!}
 main if end.file_processing = start.new defined?
   def new.start()
   create.mainFile
end
Read_main.file.read()::return::true if file.open? return nil
